{application, depmap, [
	{description, "Dependency graph for Erlang."},
	{vsn, "0.1.0"},
	{modules, []},
	{registered, [dm_sup]},
	{applications, [
		kernel,
		stdlib,
		crypto,
		public_key,
		ssl,
        mimetypes,
		cowboy,
        jsx
	]},
    {mod, {depmap, []}},
	{env, []}
]}.
